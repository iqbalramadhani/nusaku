const styles = theme => ({
  root: {
    textAlign: 'center',
    paddingTop: theme.spacing.unit * 20,
  },
  helperText : {
    color : '#da4453',
    "fontFamily": "'Open Sans', sans-serif",
    "fontSize": "10px",
  },
});

export default styles;